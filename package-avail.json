{
  "name": "avail-contract-deployment",
  "version": "1.0.0",
  "description": "Deploy smart contracts to Avail blockchain",
  "main": "scripts/deploy-avail.js",
  "scripts": {
    "build-contract": "cd contracts/stablecoin_swap && cargo contract build",
    "deploy-avail": "node scripts/deploy-avail.js",
    "test-contract": "cd contracts/stablecoin_swap && cargo test"
  },
  "dependencies": {
    "@polkadot/api": "^10.0.0",
    "@polkadot/api-contract": "^10.0.0",
    "@polkadot/util-crypto": "^12.0.0",
    "@polkadot/keyring": "^12.0.0"
  },
  "devDependencies": {
    "cargo-contract": "^3.0.0"
  }
}
